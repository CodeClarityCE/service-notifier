services:
  service-notifier:
    # For dev we keep the golang container and install devtools
    build:
      target: plugin-debug
    image: ceherzog/service-notifier-debug:latest
    ports:
      - 40000:40000
